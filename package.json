{
  "name": "enterprise-workforce-ai-platform",
  "version": "1.0.0",
  "description": "Enterprise Workforce AI Platform - Single Service Deployment",
  "main": "backend/server.js",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "install-backend": "npm install --prefix backend",
    "install-frontend": "npm install --include=dev --prefix frontend",
    "build-frontend": "npm run build --prefix frontend",
    "build": "npm run install-backend && npm run install-frontend && npm run build-frontend",
    "start": "npm start --prefix backend"
  }
}
